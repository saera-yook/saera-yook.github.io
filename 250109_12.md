# 12장 '다형성’

- 상속은 타입 계층(다형성의 기반)을 구조화하기 위해 사용해야
- 클라이언트 관점에서 인스턴스들을 동일하게 행동하는 그룹으로 묶기 위해

## 다형성
- 많은 형태를 가질 수 있는 능력
- 추상 인터페이스에 대해 서로 다른 구현을 연결할 수 있는 능력[Czarnecki00]
- 유니버설(Universal) 다형성
  - 매개변수 다형성
    - 제네릭 프로그래밍. 임의의 타입으로 선언한 후 사용하는 시점에 구체적 타입으로 지정하는 방식
  - 포함 다형성(서브타입 다형성, 일반적으로 다형성 하면 포함 다형성 의미)
    - 메시지가 동일하더라도 수신한 객체의 타입에 따라 실제로 수행되는 행동이 달라지는 능력
    - 상속으로 구현. 런타임에 상속 계층 안에서 적절한 메서드를 선택
- 임시(Ad Hoc) 다형성
  - 일반적으로 오버로딩과 강제를 함께 사용하면 모호해진다. 실제로 어떤 메서드가 호출될지 판단하기 어려움.
  - 오버로딩 다형성
    - 하나의 클래스 안에 동일한 이름의 메서드가 존재하는 경우
    - 유사한 작업을 수행하는 메서드의 이름을 통일. 기억해야 하는 이름의 수 줄임.
  - 강제(Coercion) 다형성
    - 자동 타입 변환, 직접 구현한 타입 변환을 이용해 동일한 연산자를 다양한 타입에 사용할 수 있는 방식.

## 상속의 양면성
- 업캐스팅, 동적 메서드 탐색, 동적 바인딩, self 참조, super 참조

### 상속을 사용한 강의 평가
- 부모와 자식에 동일한 시그니처를 가진 메서드가 존재할 경우 자식의 메서드 우선순위가 더 높다. 메시지 수신 시 자식의 메서드가 실행됨: 메서드 오버라이딩
- 오버로딩: 부모의 메서드와 이름 동일, 시그니처 다른 메서드를 자식 클래스에 추가. 두 메서드는 공존.

### 데이터 관점의 상속
### 행동 관점의 상속
- 런타임에 시스템이 자식 클래스에 정의되지 않은 메서드가 있으면 부모 클래스 안에서 탐색

## 업캐스팅과 동적 바인딩
### 같은 메시지, 다른 메서드
- 업캐스팅: 부모 클래스 타입으로 선언된 변수에 자식 클래스의 인스턴스를 할당하는 것
- 동적 바인딩: 선언된 변수의 타입이 아니라 메시지를 수신하는 객체의 타입에 따라 실행되는 메서드가 결정된다. 컴파일 시점 아닌 실행 시점에 결정하기 때문.
- 동일한 수신자에게 동일한 메시지를 전송하는 동일한 코드를 이용해 서로 다른 메서드를 실행 가능하게 하는 메커니즘: 업캐스팅과 동적 메서드 탐색
- 개방-폐쇄 원칙이 목적이라면 업캐스팅과 동적 메서드 탐색은 목적에 이르는 방법
- 다운캐스팅: 부모 클래스의 인스턴스를 자식 클래스 타입으로 변환하기 위해 명시적인 타입 캐스팅이 필요

## 동적 메서드 탐색과 다형성
- self 참조(자바의 this)
  - 객체가 메시지 수신하면 컴파일러는 self 참조라는 임시 변수를 자동 생성 -> 메시지 수신한 객체 가리키도록 설정
  - self가 가리키는 객체의 클래스에서 시작해 상속 계층의 역방향으로 동적 메서드 탐색
  - 탐색이 종료되는 순간 self 참조는 자동으로 소멸
- 동적 메서드 탐색의 2가지 원리
  - 자동적인 메시지 위임. 자식 -> 부모로 처리를 위임.
  - 동적인 문맥. 실행 시점에 어떤 메서드 실행할지 결정.

### 자동적인 메시지 위임
- 상속 계층을 정의 = 메서드 탐색 경로를 정의
- 지원하는 방법은 언어에 따라 다를 수 있다.
- C++은 같은 클래스 안에서의 메서드 오버로딩은 허용. 상속 계층 사이에서의 메서드 오버로딩은 금지. 
- 이름 숨기기[Eckel03]: 부모 클래스의 이름이 동일한 메서드 전체를 숨겨서 클라이언트가 호출하지 못하도록 막는다.
- 이름 숨기기 문제 해결 방법
  - 부모 클래스에 정의된 모든 메서드를 자식 클래스에서 오버로딩
  - 부모 클래스에 정의된 메서드 이름을 자식 클래스의 네임스페이스에 합침

### 동적인 문맥
- self 전송
  - self 참조가 가리키는 자기 자신에게 메시지를 전송하는 것
  - 종종 어떤 메서드가 실행될지를 예상하기 어렵게 만든다.
  - 자식에서 부모 방향으로 진행되는 동적 메서드 탐색 경로를 다시 self 참조가 가리키는 원래의 자식 클래스로 이동시킨다.
  - 최악의 경우 실제 실행될 메서드를 찾기 위해 상속 계층 전체를 훑어야
  - 깊은 상속 계층과 계층 중간중간 숨겨진 메서드 오버라이딩과 만나면 극단적으로 이해하기 어려운 코드가 만들어짐

### 이해할 수 없는 메시지
- 정적 타입 언어
  - 코드 컴파일 시 상속 계층 안의 클래스들이 메시지를 이해할 수 있는지 여부 판단. 메시지 처리 가능한 메서드 못 찾으면 컴파일 에러 발생.
  - 유연성이 부족하지만 안정적. 실행 시점에 오류가 발생할 가능성 줄임.
- 동적 타입 언어
  - 컴파일 단계 없음. 실행 전에는 메시지 처리 가능 여부를 판단 불가.
  - self 참조가 가리키는 현재 객체에게 메시지를 이해할 수 없다는 메시지 전송
      - 이 메시지 역시 보통의 메시지처럼 동적 메서드 탐색 진행. 상속 계층 안에서 처리할 수 없다면 최종적으로 예외 던져짐.
      - 스몰토크, 루비의 최상위 클래스들은 이 메시지에 대한 기본 처리로 특정 예외를 던짐
  - 메서드를 찾을 수 없다는 메시지에 응답할 수 있는 메서드를 구현
    - 자신의 인터페이스에 정의되지 않은 메시지를 처리하는 것이 가능
    - 좀 더 순수한 관점에서 객체지향 패러다임을 구현
  - 이해할 수 없는 메시지를 처리할 수 있는 능력을 가짐으로써 메시지가 선언된 인터페이스와 메서드가 정의된 구현을 분리 가능
  - 그러나 코드를 이해하고 수정하기 어렵게 만든다. 디버깅 과정을 복잡하게 만든다.
  - 동적 리셉션[Fowler10]: 이런 특징을 이용해 도메인-특화 언어를 개발하는 방식
    - 메타 프로그래밍 영역에서 진가 발휘
    - 정적 타입 언어보다 더 쉽고 강력한 도메인-특화 언어(D Specific L)를 개발할 수 있는 것으로 간주됨.

### self 대 super
- super 참조
  - 자식 클래스에서 부모 클래스의 구현을 재사용해야 하는 경우
- super 전송
  - 항상 부모 클래스에서부터 메서드 탐색을 시작하게 한다. 상속 계층 따라 계속 탐색.
  - 탐색 시작 위치를 정적으로 결정. 컴파일 시점에 결정 가능(스칼라의 트레이트 제외)

## 상속 대 위임
### 위임과 self 참조
- 자식 클래스 생성 시 개념적으로 부모 클래스 인스턴스를 내부에 포함하는 것으로 표현 가능
- 자식 인스턴스에 포함된 부모 인스턴스 입장에서 self 참조는 자식 인스턴스를 가리킴. 항상 메시지를 수신한 객체를 가리키기 때문.
- 위임 
  - 자신이 수신한 메시지를 다른 객체에게 동일하게 전달해서 처리를 요청하는 것
  - 항상 형재의 실행 문맥을 가리키는 self 참조를 인자로 전달한다.
  - 클래스를 이용한 상속 관계를 객체 사이의 합성 관계로 대체해서 다형성을 구현하는 것
  - 객체 사이의 동적인 연결 관계를 이용해 상속을 구현하는 방법
  - vs 포워딩: self 참조를 전달하지 않음. 메시지 수신한 최초의 객체에 다시 메시지를 전송할 필요는 없고 단순히 코드를 재사용하고 싶은 경우
- 상속은 실행 시에 인스턴스들 사이에서 self 참조가 자동으로 전달된다. 자식 인스턴스와 부모 인스턴스 사이에 동일한 실행 문맥을 공유 가능하게 함.
- 클래스 기반 객체지향 언어에서 객체 사이의 위임을 직접 구현하는 것은 쉽지 않음. 하지만 클래스 사이의 메시지 위임을 자동으로 처리해줌

### 프로토타입 기반의 객체지향 언어
- 클래스 없고 오직 객체만 존재(객체지향 패러다임에서 클래스가 필수 요소가 아니다.)
- 상속을 구현하는 유일한 방법은 객체 사이의 위임을 이용하는 것. 객체 사이의 self 참조를 자동으로 전달(상속 이외의 방법으로도 다형성 구현 가능하다.)
- 자바스크립트에서는 prototype 체인으로 연결된 객체 사이에 메시지를 위임함으로써 상속을 구현(거슬러 올라간다.)
  - 자바스크립트의 모든 객체들은 다른 객체를 가리키는 용도로 사용되는 prototype이라는 이름의 링크를 가진다.
  - 인스턴스는 메시지 수신 시 먼저 수신한 객체의 prototype 안에서 메서드를 탐색한다.
  - 메서드 없으면 prototype이 가리키는 객체를 따라 메시지 처리를 자동으로 위임
```javascript
Lecture.prototype.stats = function () {}
Lecture.prototype.getEvaluationMethod = function () {}
```
- 메서드를 Lecture의 prototype이 참조하는 객체에 정의했다는 점에 주목하라.
- Lecture를 이용해 생성된 모든 객체들은 prototype 객체에 정의된 메서드를 상속받는다.
- 특별한 작업을 하지 않는 한 prototype에 할당되는 객체는 자바스크립트의 최상위 객체 타입인 Object다.
- 따라서 prototype이 참조하는 Object에 정의된 모든 속성과 메서드를 상속받는다.
```javascript
GradeLecture.prototype = new Lecture();
GradeLecture.prototype.constructor = GradeLecture;
GradeLecture.prototype.getEvaluationMethod = function () {}
```
- GradeLecture의 prototype에 Lecture의 인스턴스를 할당했다는 것에 주목
- GradeLecture를 이용해 생성된 모든 객체들이 prototype을 통해 Lecture에 정의된 모든 속성과 함수에 접근 가능. 자동으로 상속받음.
- 메서드 탐색 과정은 클래스 기반 언어의 상속과 거의 동일
  - (객체)self.prototype = GradeLecture 인스턴스 / GradeLecture.prototype = Lecture 인스턴스
  - Lecture.stats()에서 this.getEvaluationMethod() 만나면 self 참조가 가리키는 현재 객체에서부터 다시 메서드 탐색 시작
  - Lecture.stats() 안의 this는 Lecture의 인스턴스가 아니다. 메시지를 수신한 현재 객체를 가리킨다.
- 정적인 클래스 간의 관계가 아니라 동적인 객체 사이의 위임을 통해 상속을 구현