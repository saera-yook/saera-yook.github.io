# 7장 '객체 분해’

## 프로시저 추상화와 데이터 추상화

- 프로시저 추상화: 소프트웨어가 무엇을 해야하는지를 추상화
- 데이터 추상화: 소프트웨어가 무엇을 알아야 하는지를 추상화
- 소프트웨어는 데이터를 이용해 정보를 표현하고 프로시저를 이용해 데이터를 조작한다.
- 시스템 분해 방법
    - 프로시저 추상화 중심으로: 기능 분해, 알고리즘 분해
    - 데이터 추상화 중심으로
        - 데이터를 중심으로 타입을 추상화: 추상 데이터 타입
        - 데이터를 중심으로 프로시저를 추상화: 객체지향
            - 프로그래밍 언어 관점에서 객체지향은 데이터 추상화와 프로시저 추상화를 함께 포함한 클래스를 이용해 시스템을 분해하는 것

## 프로시저 추상화와 기능 분해

### 메인 함수로서의 시스템

- 기능 분해 방법
- 프로시저는 반복, 유사한 작업들을 하나의 장소에 모아 로직을 재사용하고 중복 방지 가능한 추상화 방법
- 내부 구현 몰라도 인터페이스만 알면 프로시저 사용 가능
- 시스템은 입력 값을 계산해서 출력 값을 반환하는 수학의 함수와 동일. 더 작은 작업으로 분해될 수 있는 하나의 커다란 메인 함수
- 하향식 접근법: 먼저 필요한 기능을 생각 -> 분해 및 정제 과정에서 필요한 데이터의 종류와 저장 방식을 식별 => 유지보수 문제 야기
- 시스템의 기능을 표현하는 하나의 문장으로 나타낸 후 세분화. 추상적인 최상위 문장을 기술.-> 시스템을 시작하는 메인 프로시저로 구현

### 하향식 기능 분해의 문제점

- 시스템은 하나의 메인 함수로 구성돼 있지 않다.
    - 하나의 알고리즘이나 배치 처리를 구현하기에는 적합
    - 동등한 수준의 다양한 기능으로 구성되는 현대적인 상호작용 시스템 개발에는 적합하지 않음
- 메인 함수의 빈번한 재설계
    - 새로운 기능 추가할 때마다 매번 메인 함수를 수정해야
- 비즈니스 로직과 사용자 인터페이스 로직의 결합
    - 시스템 내에서 가장 자주 변경되는 사용자 인터페이스를 변경할 때마다 비즈니스 로직까지 영향 받음. 변경에 불안정한 아키텍처
    - 기능 분해 과정에서 두 로직을 동시에 고려하도록 강요하기 때문에 분리가 어렵다.
- 성급하게 결정된 실행 순서. 유연성, 재사용성 저하
    - 기능 분해 과정이 시스템이 무엇을 해야 하는지가 아니라 어떻게 동작해야 하는지에 집중하게 만듦
    - 처음부터 구현을 염두에 두기 때문에 함수들의 실행 순서를 정의하는 시간 제약을 강조. 함수의 순서를 결정해야 작게 분해 가능
    - 실행 순서, 조건, 반복과 같은 제어 구조를 미리 결정하지 않고는 분해 불가능 -> 중앙집중 제어 스타일
    - 논리적 제약을 설계 기준으로 삼아야. 객체지향은 객체 사이의 논리적 관계를 중심으로 설계
    - 재사용 불가능: 재사용은 일반성 개념을 포함. 하위 함수는 항상 상위 함수보다 문맥에 더 종속적
- 모든 문제의 원인은 결합도
- 데이터 형식 변경 시 파급효과 예측 불가능
    - 전체 시스템의 핵심적인 구조를 결정하는 함수들이 데이터와 강하게 결합
    - 어떤 데이터를 어떤 함수가 사용하고 있는지 추적 어려움
    - 의존성과 결합도의 문제. 테스트 어려움.

### 언제 하향식 분해가 유용한가?

- 이미 완전히 이해된 사실을 서술하기에 적합
    - 작은 프로그램, 개별 알고리즘을 위해 유용한 패러다임
    - 이미 해결된 알고리즘을 문서화하고 서술하는 데 훌륭
- 새로운 것을 개발하고, 설계하고, 발견하는 데는 적합한 방법이 아님
    - 설계가 어느 정도 안정화 된 후에 설계의 다양한 측면을 논리적으로 설명하고 문서화하기에 용이 -> 오해를 불러일으킴

## 모듈

### 정보 은닉과 모듈

- 정보 은닉[데이비드 파나스72]
    - 시스템을 모듈 단위로 분해하기 위한 기본 원리
    - 자주 변경되는 부분을 상대적으로 덜 변경되는 안정적인 인터페이스 뒤로 감춰야 한다는 것이 핵심
- 모듈과 기능 분해는 상호 배타적인 관계가 아님. 시스템을 모듈로 분해 -> 모듈 내부 구현을 위해 기능 분해를 적용
- 모듈이 감춰야 할 비밀
    - 복잡성
    - 변경 가능성
- 시스템의 가장 일반적인 비밀은 데이터 -> 데이터 캡슐화와 정보 은닉을 혼동하게 만듦
    - 비밀이 반드시 데이터일 필요는 없음
    - 복잡한 로직이나 변경 가능성이 큰 자료 구조일 수도 있음
    - 데이터 캡슐화는 비밀의 한 종류인 데이터를 감추는 캡슐화의 한 종류
- 자바에서는 package를 이용해 모듈 구현 가능

### 모듈의 장점과 한계

- 장점
    - 모듈 내부의 변수가 변경되더라도 모듈 내부에만 영향을 미친다.
    - 비즈니스 로직과 사용자 인터페이스에 대한 관심사를 분리한다.
    - 전역 변수와 전역 함수를 제거함으로써 네임스페이스 오염을 방지한다.
- 단점
    - 인스턴스의 개념을 제공하지 않는다.
    - 더 높은 수준의 추상화 메커니즘이 필요 -> 추상 데이터 타입의 등장

## 데이터 추상화와 추상 데이터 타입

### 추상 데이터 타입

- 프로그래밍 언어에서 타입: 변수에 저장할 수 있는 내용물의 종류와 변수에 적용될 수 있는 연산의 가짓수를 의미
- 프로시저 추상화는 프로그램의 표현력을 향상시키는 데 한계가 있다.
- 데이터 추상화[바바라 리스코프74]: 소프트웨어로 표현할 수 있는 추상화의 수준을 한 단계 높였다.
- 추상 데이터 타입 기반으로 객체 생성 가능하지만 여전히 데이터와 기능을 분리해서 바라본다.
    - 추상 데이터 타입으로 표현된 데이터를 이용해서 기능을 구현하는 핵심 로직은 추상 데이터 타입 외부에 존재

## 클래스

### 클래스는 추상 데이터 타입인가?

- 클래스는 상속과 다형성을 지원하는 데 비해 추상 데이터 타입은 그렇지 못하다.
- 객체지향 프로그래밍과 구분하기 위해 추상 데이터 타입 기반의 프로그래밍 패러다임을 객체기반 프로그래밍이라고 부르기도
- 추상 데이터 타입은 오퍼레이션을 기준으로 타입을 추상화한 것 vs 클래스는 타입을 기준으로 절차를 추상화한 것

### 변경을 기준으로 선택하라

- 클래스를 사용하고 있더라도 타입을 기준으로 추상화하지 않았다면 그것은 객체지향 분해가 아니다.
- 클래스가 추상 데이터 타입의 개념을 따르는지 확인하는 방법
    - 클래스 내부에 인스턴스의 타입을 표현하는 변수가 있는지 살펴보는 것
    - 인스턴스 변수에 저장된 값을 기반으로 메서드 내에서 타입을 명시적으로 구분하는 방식은 객체지향을 위반하는 것
    - e.g. Employee 클래스에서 hourly라는 인스턴스 변수에 직원의 유형을 저장하는 것
- 객체지향에서는 타입 변수를 이용한 조건문을 다형성으로 대체 -> 변경 때문에 조건문 기피
- 개방-폐쇄 원칙[Martin02]: 기존 코드에 아무 영향 미치지 않고 새로운 객체 유형과 행위를 추가할 수 있는 객체지향의 특성
- 변경의 압력이 타입 추가에 관한 것이면 객체지향, 오퍼레이션 추가에 관한 것이면 추상 데이터 타입
- 책임을 다양한 방식으로 수행해야 할 때만 타입 계층 안에 각 절차를 추상화하라(다형성)