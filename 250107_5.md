# 5장 '책임 할당하기’

## 책임 주도 설계를 향해

### 협력이라는 문맥 안에서 책임을 결정하라
- 객체 입장에서 책임이 조금 어색해 보이더라도 협력에 적합하다면 그 책임은 좋은 것이다. 책임은 객체가 아닌 협력에 적합해야.
- 협력 시작 주체는 메시지 전송자. 협력에 적합한 책임 = 메시지 전송자에게 적합한 책임. 수신자X
- 클라이언트의 의도에 적합한 책임을 할당해야. 메시지가 클라이언트의 의도를 표현. 클라이언트는 어떤 객체가 수신할지 모름. 메시지 수신자에 대한 가정 불가능

## 책임 할당을 위한 GRASP 패턴[Larman04]
- General Responsibility Assignment Software Pattern, 크레이그 라만

### 도메인 개념에서 출발하기
- 도메인 개념들을 책임 할당의 대상으로 사용 -> 코드에 도메인을 투영하기 수월
- 도메인 개념은 설계 시작 시 참고 대상. 정리하는 데 너무 많은 시간을 들이지 말고 빠르게 설계와 구현을 진행하라.
- 도메인 모델은 구현을 염두에 두고 구조화되는 것이 바람직. 도메인을 그대로 투영한 모델이 아니라 구현에 도움이 되는 모델이 필요. 즉, 실용적, 유용한 모델

### 정보 전문가에게 책임을 할당하라 - INFORMATION EXPERT(정보 전문가) 패턴
- 메시지를 전송할 객체는 무엇을 원하는가?
- 메시지를 수신할 적합한 객체는 누구인가?
- 정보는 데이터와 다르다. 정보 전문가가 정보를 알고 있다고 해서 그 데이터를 반드시 저장하고 있을 필요는 없다.
    - 정보를 제공할 수 있는 다른 객체를 알고 있거나, 필요한 정보를 계산해서 제공할 수도 있다.

### 높은 응집도와 낮은 결합도 - HIGH COHESION 패턴, LOW COUPLING 패턴
- 책임 협력의 품질 검토 평가 기준
- 책임 할당, 코드 작성 매 순간마다 낮은 결합도, 높은 응집도 관점에서 전체적인 설계 품질을 검토하고 선택하라.

### 창조자에게 객체 생성 책임을 할당하라 - CREATOR(창조자) 패턴
- 협력의 최종 결과물이 인스턴스 생성이라면 협력에 참여하는 어떤 객체에게는 인스턴스를 생성할 책임을 할당해야 한다는 것
- CREATOR 패턴: 아래 조건을 최대한 많이 만족하는 B에게 객체 생성 책임을 할당하라.
    - a객체를 포함하거나 참조한다.
    - a객체를 기록한다.
    - a객체를 긴밀하게 사용한다.
    - a객체를 초기화하는데 필요한 데이터를 가지고 있다.(이 경우 B는  A에 대한 정보 전문가다)
    - 이미 결합돼 있는 객체에게 생성 책임을 할당
- 올바르게 설계하고 있는지 궁금한가? 코드를 작성하라.


## 구현을 통한 검증
- 변경 이유가 하나 이상인 클래스를 찾아 변경의 이유에 따라 클래스를 분리
  - 인스턴스 변수가 초기화되는 시점 -> 함께 초기화되는 속성을 기준으로 코드를 분리
    - 응집도 높은: 인스턴스 생성 시 모든 속성을 함께 초기화
    - 응집도 낮은: 속성 중 일부만 초기화
  - 메서드들이 인스턴스 변수를 사용하는 방식 -> 속성 그룹과 해당 그룹에 접근하는 메서드 그룹을 기준으로 코드를 분리
    - 높은: 모든 메서드가 객체의 모든 속성을 사용
    - 낮은: 메서드들이 사용하는 속성에 따라 그룹이 나뉜다

### 타입 분리하기
- 두 개의 독립적인 타입이 하나의 클래스 안에 공존한다면 두 개의 클래스로 분리
- 분리 결과로 어떤 인스턴스가 두 개의 서로 다른 클래스의 인스턴스와 모두 협력해야 한다면
  - 두 클래스 목록을 따로 갖는 것
    - 한 클래스가 두 클래스 모두에게 결합됨. 결합도 높아짐. 
    - 변경 어려워짐. 캡슐화 위반.

### 다형성을 통해 분리하기 - POLYMORPHISM(다형성) 패턴
- 두 클래스가 동일한 책임을 수행한다 -> 동일한 역할이다. 역할은 대체가능성 의미.
- 구체적인 클래스는 알지 못한 채 역할에 대해서만 결합되도록 의존성 제한
- 협력이 다형적으로 되었다.
- POLYMORPHISM(다형성) 패턴
  - 객체의 타입에 따라 변하는 행동이 있다면 타입을 분리하고 변화하는 행동을 각 타입의 책임으로 할당
  - 타입을 명시적으로 정의하고 각 타입에 다형적으로 행동하는 책임을 할당 -> 다형성을 이용해 변경 용이하게 확장
  - 객체의 타입을 검사해서 타입에 따라 여러 대안들을 수행하는 조건적인 논리를 사용하지 말라고 경고
  - 프로그램을 if ~ else 또는 switch ~ case 등의 조건 논리를 사용해서 설계한다면 변경 시 조건 논리를 수정해야 함 -> 변경 취약

### 변경으로부터 보호하기- PROTECTED VAIRATIONS(변경보호) 패턴
- 추상화가 구체적인 타입을 캡슐화한다. -> 새로운 타입을 추가하더라도 결합된 객체가 영향을 받지 않는다.
- 변경을 캡슐화하도록 책임을 할당하는 패턴. 책임 할당 관점에서의 캡슐화.
  - 변화가 예상되는 불안정한 지점들을 식별하고 그 주위에 안정된 인터페이스를 형성하도록 책임 할당
  - 설계에서 변하는 것이 무엇인지 고려하고 변하는 개념을 캡슐화하라[GOF94]

### 변경과 유연성
- 변경에 대비하는 두 가지 방법
  - 코드를 이해하고 수정하기 쉽도록 최대한 단순하게 설계하는 것
  - 코드를 수정하지 않고도 변경을 수용할 수 있도록 유연하게 만드는 것
    - 유사한 변경이 반복 -> 복잡성이 상승하더라도 유연성을 추가하는 것이 더 좋다.
- 상속 대신 합성을 사용하여 유연한 설계로 변경
  - 새로운 정책이 추가되어도 추가 코드를 작성할 필요 없음
  - 새로운 클래스를 추가하고 클래스의 인스턴스를 전달하면 됨
- 유연성은 의존성 관리의 문제. 의존성의 정도가 유연성의 정도를 결정. 결합도를 조절할 수 있다.

## 책임 주도 설계의 대안
- 최대한 빠르게 목적한 기능을 수행하는 코드를 작성 -> 코드 상에 명확하게 드러나는 책임들을 올바른 위치로 이동: 데이터 중심으로 구현 후 리팩터링
- 몬스터 매서드[Feathers04]: 긴 메서드는 응집도 낮음, 이해 어려움, 재사용 어려움, 변경 어려움.
- 작은 메서드는 실제로 이름을 잘 지었을 때만 그 진가가 드러나므로, 이름을 지을 때 주의해야 한다. 길이가 아니라 중요한 것은 메서드의 이름과 메서드 몸체의 의미적 차이다.[Fowler99a]
- 메서드를 작고 응집도 높게 분리하면 적절한 클래스로 이동하기가 더 수월
- 일반적으로 명확성의 가치가 클래스의 길이보다 더 중요하다.

### 객체를 자율적으로 만들자
- 메서드가 어떤 데이터를 사용하는지 알려면 메서드 안에서 어떤 클래스의 접근자 메서드를 사용하는지 파악
- 메서드를 다른 클래스로 이동 시 인자에 정의된 클래스 중 하나로 이동하는 경우가 일반적