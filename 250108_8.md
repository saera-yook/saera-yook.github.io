# 8장 '의존성 관리하기’
객체지향 설계의 핵심: 협력을 위해 필요한 의존성은 유지하면서도 변경을 방해하는 의존성은 제거

## 의존성 이해하기

### 변경과 의존성
의존성의 의미
- 실행 시점: 의존하는 객체가 정상적으로 동작하기 위해서는 실행 시에 의존 대상 객체가 반드시 존재해야 한다.
- 구현 시점: 의존 대상 객체가 변경될 경우 의존하는 객체도 함께 변경된다. 그 역은 성립하지 않는다.
- 의존성은 방향성을 가지며 항상 단방향이다.
- 변경에 의한 영향의 전파 가능성을 암시. 두 요소 사이에 변경에 의해 영향을 주고받는 힘의 역학관계가 존재한다.

### 의존성 전이
- 의존 대상 객체가 의존하는 대상에 대해서도 의존하게 된다. 의존성 전파. 항상 그런 건 아님. 경고일 뿐.
- 의존성이 실제로 전이될지 여부는 변경의 방향과 캡슐화의 정도에 따라 달라진다.
- 직접 의존성: 한 요소가 다른 요소에 직접 의존하는 경우
- 간접 의존성: 직접적인 관계는 존재하지 않지만 의존성 전이에 의해 영향이 전파되는 경우. 의존하는 객체의 코드 안에 명시적으로 드러나지 않는다.

### 런타임 의존성과 컴파일타임 의존성
- 런타임 의존성: 객체 사이의 의존성
- 컴파일타임 의존성: 코드 작성 시점. 코드의 구조. 코드 그 자체. 클래스 사이의 의존성
- 컴파일타임 구조와 런타임 구조 사이의 거리가 멀면 멀수록 설계가 유연, 재사용 가능해진다.
- 유연, 재사용 가능한 코드를 위해서는 두 종류의 의존성을 서로 다르게 만들어야. 동일한 소스코드 구조를 가지고 다양한 실행 구조를 만들 수 있어야
- 추상 클래스에 대한 컴파일타임 의존성을 실행 시에 다른 클래스의 인스턴스에 대한 런타임 의존성으로 대체해야
- 다양한 클래스의 인스턴스와 협력하기 위해서는 협력할 인스턴스의 구체적인 클래스를 알아서는 안 된다.

### 컨텍스트 독립성
- 구체 클래스에 대해 의존하는 것은 그 클래스가 사용되는 특정한 문맥에 강하게 결합되는 것 -> 다른 문맥에서 사용하기 어려워진다.
- 가능한 한 자신이 실행될 컨텍스트에 대한 구체적인 정보를 최대한 적게 알아야. 시스템에 관해 알지 못한다.

### 의존성 해결하기
- 컴파일타임 의존성을 실행 컨텍스트에 맞는 적절한 런타임 의존성으로 교체하는 것
- 객체 생성 시점에 생성자를 통해
- 객체 생성 후 setter 메서드를 통해
  - 객체 생성 이후에도 의존 대상을 변경할 수 있는 가능성을 열어 놓고 싶을 경우 유용
  - 객체 생성 후 의존 대상을 설정하기 전까지는 객체의 상태가 불완전할 수 있다.
  - setter 방식과 생성자 방식을 혼합
- 메서드 실행 시 인자를 이용해
  - 메서드가 실행되는 동안만 일시적으로 의존 관계가 존재해도 무방한 경우에 유용
  - 메서드가 실행될 때마다 의존 대상이 매번 달라져야 하는 경우에 유용
  - 매번 동일한 객체를 인자로 전달하고 있다면 의존성을 지속적으로 유지하는 방식으로 변경하는 것이 좋다.


## 유연한 설계

### 의존성과 결합도
- 의존성은 협력을 위해 반드시 필요하다. 바람직하지 못한 의존성이 문제일 뿐.
- 바람직한 의존성은 재사용성과 관련 -> 결합도
  - 어떤 의존성이 다양한 환경에서 클래스를 재사용할 수 있다면 바람직. 반대는 바람직하지 못한
  - 즉, 컨텍스트에 독립적인 의존성
  - 다른 환경에서 재사용하기 위해 내부 구현을 변경하게 만드는 모든 의존성은 바람직하지 않다.
  - 바람직: 느슨한, 약한 결합도 & 지식의 양이 적다 / 단단한, 강한 결합도
  - 의존성은 관계 유무를 설명. 존재한다. 존재하지 않는다.
  - 결합도는 존재하는 의존성의 정도를 상대적으로 표현. 강하다. 느슨하다.

### 추상화에 의존하라
- 구체 클래스 의존성 > 추상 클래스 의존성 > 인터페이스 의존성
- 실행 컨텍스트에 대해 알아야 하는 정보를 줄일수록, 더 추상적일수록 결합도는 더 낮아진다.

### 명시적인 의존성
- 클래스 안에서 구체 클래스에 대한 모든 의존성을 제거해야 한다.
- 인스턴스 변수의 타입은 추상 클래스나 인터페이스로 정의하고 의존성을 해결할 때는 구체 클래스를 전달
- 명시적인 의존성
  - 의존성의 대상을 생성자(setter, 메서드)의 인자로 전달받는 방법
  - 의존 사실을 퍼블릭 인터페이스에 드러내는 것. 의존성이 명시적으로 노출된다.
- 숨겨진 의존성
  - 내부에서 인스턴스를 직접 생성하는 방식
  - 의존 사실을 감춘다. 의존성이 퍼블릭 인터페이스에 표현되지 않는다.
  - 의존성 파악하려면 내부 구현을 직접 살펴봐야. 쉽지 않다.
  - 재사용하기 위해 내부 구현을 직접 변경해야
- 의존성은 명시적으로 표현돼야 한다. 구현 내부에 숨기지 마라. 의존성을 감추는 것을 경계해야

### new는 해롭다
잘못 사용하면 결합도가 극단적으로 높아진다.
- 사용하려면 구체 클래스의 이름을 직접 기술해야 함. 사용하는 클라이언트는 구체 클래스에 의존할 수 밖에 없다.
- 구체 클래스뿐만 아니라 어떤 인자를 이용해 생성자를 호출하는지도 알아야 한다. 클라이언트가 알아야 하는 지식의 양이 늘어난다.
- 의존해야 하는 유일한 이유는 메시지를 전송하기 위해. 그 외의 다른 모든 의존성은 결합도를 높이는 불필요한 의존성
- 인스턴스를 생성 로직과 생성된 인스턴스 사용 로직을 분리
- 그렇다면 누가 생성? 사용하는 객체의 클라이언트가 처리한다. 책임의 이동.

### 가끔은 생성해도 무방하다
- 클래스 안에서 객체의 인스턴스를 직접 생성하는 방식이 유용한 경우도 있다.
  - 구체 클래스에 의존하게 되더라도 클래스 사용성이 더 중요하다면 결합도를 높이는 방향으로 작성 사능.
- 주로(대부분의 경우) 협력하는 기본 객체를 설정하고 싶은 경우
- 생성자 체이닝
  - 첫 번째 생성자 내부에서 두 번째 생성자를 호출
- 메서드 오버로딩
- 결합도와 사용성을 트레이드오프

### 표준 클래스에 대한 의존은 해롭지 않다
- 변경될 확률이 거의 없는 클래스라면 의존성이 문제되지 않는다.
- 그럼에도 추상화에 의존하고 의존성을 명시적으로 드러내라

### 컨텍스트 확장하기
- 인스턴스 변수에 null 값 추가 -> 지금까지의 협력 방식에 어긋나는 예외 케이스. 내부 코드를 직접 수정해야 함.
- null을 하나의 종류로 간주하고 타입으로 클래스 정의
- 중복 할인 적용도 마찬가지. 새로운 타입으로 정의하고 List를 인스턴스 변수로 갖도록.

### 조합 가능한 행동
- 유연하고 재사용 가능한 설계는 작은 객체들의 행동을 조합함으로써 새로운 행동을 이끌어낼 수 있는 설계.
- 훌륭한 객체지향 설계는 객체들의 조합을 선언적으로 표현함으로써 객체들이 무엇을 하는지를 표현하는 설계
  - 클래스 인스턴스를 생성하는 코드를 보는 것만으로 객체가 어떤 일을 하는지를 쉽게 파악 가능
  - 로직을 해석할 필요 없이 객체의 연결을 보는 것만으로도 객체의 행동을 쉽게 예상하고 이해 가능 -> 선언적으로 객체의 행동을 정의 가능
- 핵심은 의존성을 관리하는 것
- 시스템에 포함된 객체의 구성을 변경해(절차적 코드 작성X. 인스턴스 추가나 제거 또는 조합을 달리해서) 시스템의 작동 방식을 바꿀 수 있다.[Freeman09]
  - 이렇게 객체 구성을 관리할 목적으로 작성하는 코드를 객체 네트워크의 행위에 대한 선언적인 정의라고 한다.
  - 방법이 아니라 목적에 집중할 수 있어 시스템의 행위를 변경하기가 더 쉽다
